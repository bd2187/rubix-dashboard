import React from "react";
import styled from "styled-components";

export const Nav_transition = styled.div`
    transition: left 0.2s ease-in-out;
`;

export const flex_center_all = `
    display: flex;
    justify-content: center;
    align-items: center;
`;

export const pixels_to_move = "250px";
